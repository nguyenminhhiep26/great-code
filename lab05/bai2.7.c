#include <stdio.h>
int main()
{
	int a;
	int b;
	int tich;
	printf ("nhap so a :");
	scanf("%d", &a);
	printf ("nhap so b :");
	scanf("%d", &b);
	
	tich=a*b;
	if (tich=a*b==1000)
	printf ("thi tich 2 so bang 1000");
	else  
	printf (" thi 2 so se lon hon 1000 ");
	 return 0;
	
	
}
