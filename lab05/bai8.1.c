#include <stdio.h>
int main()
{
	float x;
	int y;
	printf ("nhap mot so x :");
	scanf ("%f",&x);
	printf ("nhap mot so y :");
	scanf ("%d", &y);
	
	if ("%f", x<2000 || x>3000)
		printf ("thoa man x ", x<2000 || x>3000);
	
	else 
	printf ("khong thao man x\n");
	
	if ("%d", y>100 && y<500)
	  printf("thoa man y", y>100 && y<500);
	else 
	 printf ("khong thoa man y");

	 
	 
	 return 0;
	
}
