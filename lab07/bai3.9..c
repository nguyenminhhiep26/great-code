#include <stdio.h>
main()
{
	int num1, num2, i, sum;
	
	printf ("nhap so :\n");
	scanf ("%d", &num1);
	printf ("nhap so :\n");
	scanf ("%d", &num2);
	sum = i;
	for (i = num1 ; i <= num2 ; i+=sum)
	 if (num1 < num2)
	{		
	
		{
			sum = sum + i ;
			printf ("%d", i);
			
		}
			printf (" %d ", sum);
	}

}
