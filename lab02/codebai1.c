#include <stdio.h>

int main()

{
	int a;
	printf (" Nhap vao 1 so = ");
	 scanf ("%d", &a);
	 	printf (" la mot so am = %d\n " ,a );
	 if (a < 0){
	 }
	 
	  if ( a > 0){
	  	printf (" la mot so nguyen = %d\n" ,a);
	  }
	  else 
	  printf (" khong la mot so nguyen am = %d " ,a);
	  
	  return 0;
}
